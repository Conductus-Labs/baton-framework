# Frontmatter Parse Error

❌ Error: Invalid frontmatter syntax in project manifest

**File:** `.baton/project.manifest.md`

**Recovery:**
- Verify frontmatter YAML syntax is correct
- Check that frontmatter is properly delimited with `---`
- Ensure all YAML keys and values are properly formatted

