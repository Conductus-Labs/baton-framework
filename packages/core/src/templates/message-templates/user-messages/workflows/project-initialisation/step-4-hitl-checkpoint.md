# HITL Checkpoint: Review and Complete Project Configuration

🔍 HITL Checkpoint: Review and Complete Project Configuration

I've scanned your project and estimated the following values for project.config.yml:

**Estimated Values (from project scan):**
{Display estimated values with confidence indicators - these are already filled in}

**⚠️ REQUIRED FIELDS - Must be completed before approval:**
{For each required field with placeholder, display:}
- **{field_path}**: {field_description}
  - Current value: {placeholder or empty}
  - Example values: {provide 2-3 examples}
  - Valid options: {if applicable, list valid options}
  - Guidance: {brief explanation of what to enter}

**Optional Fields (can be left empty or filled later):**
{List optional fields with placeholders}

**Default Values (already set, can be modified):**
{List fields with default values, e.g., preferences section}

**Current Configuration Preview:**
{Display current project.config.yml content with estimated values filled in, highlighting required fields that still need input}

**⚠️ IMPORTANT:**
- You CANNOT approve until all REQUIRED fields are filled
- If you respond with "approve" but required fields still have placeholders, I will ask you to provide those values
- Default values (like preferences) are already set and don't need input unless you want to change them

**Action Required:**
- ✅ Approve: Use these values and create/update project.config.yml (ONLY if all required fields are filled)
- ❌ Reject: Cancel project initialization
- ✏️ Modify: Provide corrections or additional values for required/optional fields

**⚠️ VALIDATION RULES:**
- If you respond with "approve" but ANY required field still contains a placeholder, I will:
  1. REJECT your approval
  2. Display an error message listing all missing required fields
  3. Ask you to provide values for those fields
  4. Re-present this checkpoint for your review
- Approval will ONLY be accepted when ALL required fields have actual values (no placeholders)

Please provide values for all REQUIRED fields before approving.  
Respond with: approve (if all required fields filled), reject, or provide modifications.

