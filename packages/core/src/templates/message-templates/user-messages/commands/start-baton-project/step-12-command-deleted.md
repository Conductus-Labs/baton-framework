# Command Deleted

The `start-baton-project` command has been deleted as it is a run-once-only command.

**Deleted Files:**
{deleted_files_list}

**Note:** {deletion_note}

This command cannot be run again. If you need to reinitialize your project, you can use:
- `/project-init` - Initialize project configuration
- `/init-baton-agent` - Initialize baton-agent

