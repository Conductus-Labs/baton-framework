# ⚠️ Warning: CLI Command Failed

The CLI command `{command}` failed or is not available.

**Fallback:** Reading directly from source directories instead.

**Command:** `{command}`  
**Error:** {error_message}

This is not a critical error - the process will continue using direct file access.

