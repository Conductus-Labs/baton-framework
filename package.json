{
  "name": "baton-framework",
  "version": "1.0.0",
  "private": true,
  "description": "Baton Framework - Core component library for AI agent orchestration",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:ordered",
    "build:ordered": "npm run build --workspace=packages/core && npm run build --workspace=packages/agents && npm run build --workspace=packages/cognitive-patterns && npm run build --workspace=packages/knowledge && npm run build --workspace=packages/workflows",
    "test": "npm run test:root && npm run test:workspaces",
    "test:root": "vitest run",
    "test:workspaces": "npm run test --workspaces",
    "test:watch": "vitest",
    "test:coverage": "npm run test:coverage:root && npm run test:coverage --workspaces",
    "test:coverage:root": "vitest run --coverage",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces",
    "publish:all": "npm run publish --workspaces --if-present"
  },
  "keywords": [
    "baton",
    "framework",
    "ai",
    "agent",
    "cognitive-patterns",
    "workflows"
  ],
  "author": "Conductus Labs Ltd",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.3.3",
    "vitest": "^1.6.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Conductus-Labs/baton-framework.git"
  }
}
