{
  "name": "baton-framework",
  "version": "1.0.0",
  "private": true,
  "description": "Baton Framework - Core component library for AI agent orchestration",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:ordered",
    "build:ordered": "npm run build --workspace=packages/core && npm run build --workspace=packages/cognitive-patterns && npm run build --workspace=packages/agents && npm run build --workspace=packages/knowledge && npm run build --workspace=packages/workflows && npm run build --workspace=packages/claude-commands && npm run build --workspace=packages/cursor-commands-core",
    "test": "npm run test:root && npm run test:workspaces",
    "test:root": "vitest run",
    "test:workspaces": "npm run test --workspaces --if-present",
    "test:watch": "vitest",
    "test:coverage": "npm run test:coverage:root && npm run test:coverage --workspaces",
    "test:coverage:root": "vitest run --coverage",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces",
    "publish:all": "npm run publish --workspaces --if-present"
  },
  "keywords": [
    "baton",
    "framework",
    "ai",
    "agent",
    "cognitive-patterns",
    "workflows"
  ],
  "author": "Conductus Labs Ltd",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "@vitest/coverage-v8": "^1.6.1",
    "eslint": "^8.57.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Conductus-Labs/baton-framework.git"
  }
}
