# Warning: Project Not Initialized (baton-agent)

⚠️ Warning: Project not initialized

Project configuration files are missing or in default/template state (containing placeholders like {project-name}, {YYYY-MM-DD}, etc.).

**Note:** Even if `.baton/project.config.yml` exists, it is NOT initialized if it contains placeholder values. The file must be populated with actual project values via `/project-init`.

After baton-agent is loaded, you MUST run /project-init to initialize the project before proceeding.

Continuing with baton-agent initialization...
