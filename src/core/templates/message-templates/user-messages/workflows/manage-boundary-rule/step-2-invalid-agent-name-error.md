# Error: Invalid Agent Name

❌ Error: Invalid agent name

**Agent Name:** {agent_name}

**Issue:** The provided agent name is invalid or does not match the expected format.

**Valid Format:**
- Agent names should match the agent short name from project.config.yml
- Agent names should be lowercase with hyphens (e.g., "baton-agent", "cli-engineer")

Please verify the agent name is correct and matches an agent in project.config.yml.

