# Select Agents to Install

Please select which agents you'd like to install. You can:
- Type agent names separated by commas (e.g., `frontend-agent,backend-agent,qa-agent`)
- Type `all` to install all suggested agents
- Type `none` to skip
- Select ANY agent from the list below, not just the suggested ones

**All Available Agents:**
{agents_list_with_descriptions}

**Note:** Agents marked as "(already installed)" are already in your project but can be selected again if needed.

**Your selection:** 

